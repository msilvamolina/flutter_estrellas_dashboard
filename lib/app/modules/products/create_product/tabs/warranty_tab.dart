import 'package:flutter/material.dart';

class ProductWarrantyTab extends StatelessWidget {
  const ProductWarrantyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ProductWarrantyTab'),
    );
  }
}
