import 'package:flutter/material.dart';

class ProductDescriptionTab extends StatelessWidget {
  const ProductDescriptionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ProductDescriptionTab'),
    );
  }
}
